<div class="container-fluid mt--6">
  <div class="row mt--5">
    <div class="col-md-8 ml-auto mr-auto">
      <div class="card card-upgrade">
        <div class="titel_show_course">
          <p class="item_title"><%= t("views.course.show.header") %></p>
          <p> <p>
          <%= link_to t("views.course.show.back"), trainee_courses_path, class: "btn btn-info" %>
        </div>
        <div class="card-body">
          <div class="table-responsive table-upgrade">
            <table class="table">
              <thead>
                <tr>
                  <th></th>
                  <th class="text-center"><%= t("views.course.show.content") %></th>
                </tr>
              </thead>
              <tbody>
                <tr>
                  <td><%= t("views.course.show.name") %></td>
                  <td class="text-center"><%= (@course.name) %></td>
                </tr>
                <tr>
                  <td><%= t("views.course.show.description") %></td>
                  <td class="text-center"><%= @course.description %></td>
                </tr>
                <tr>
                  <td><%= t("views.course.show.start_date") %></td>
                  <td class="text-center"><%= l(@course.start_date) %></td>
                </tr>
                <tr>
                  <td><%= t("views.course.show.end_date") %></td>
                  <td class="text-center"><%= l(@course.end_date) %></td>
                </tr>
                <tr>
                  <td><%= t("views.course.show.status") %></td>
                  <td class="text-center"><%= @course.status %></td>
                </tr>
              </tbody>
            </table>
            <hr class="my-4">
          </div>
        </div>
      </div>
     
      <div class="card bg-default shadow">
        <div class="card-header bg-transparent border-0">
          <h3 class="text-white mb-0"><%= t("views.course.show.header_list_subject") %></h3>
        </div>
        <div class="table-responsive">
          <table class="table align-items-center table-dark table-flush">
            <thead class="thead-dark">
              <tr>
                <th scope="col" class="sort" data-sort="name"><%= t("views.course.show.name_subject") %></th>
                <th scope="col" class="sort" data-sort="budget"><%= t("views.course.show.start_date") %></th>
                <th scope="col" class="sort" data-sort="budget"><%= t("views.course.show.end_date") %></th>
                <th scope="col" class="sort" data-sort="budget"><%= t("views.course.show.duration") %></th>
                <th scope="col" class="sort" data-sort="budget"><%= t("views.course.show.status") %></th>
                <th scope="col" class="sort" data-sort="budget"><%= t("views.course.show.option") %></th>
              </tr>
            </thead>
            <tbody class="list">
              <%= render partial: "subject_of_course", collection: @course_subjects, as: :course_subject %>
            </tbody>
          </table>
          <div class="digg_pagination">
            <%= paginate @course_subjects %>
          </div>
        </div>
      </div>
    </div>
  </div>
